# TODO

- [ ] Add confirmation dialog (AlertDialog) before executing admin delete actions in `AdminDashboard`.
  - [ ] Wrap deleteGroup, deleteScript, deleteAdGroup, deleteAdTemplate calls with a shared `_confirmDelete` helper.
- [x] Run `flutter analyze` (warnings/info only; no compile blockers reported).
- [x] Run `flutter test` (optional, best-effort) to confirm no widget-level issues.

